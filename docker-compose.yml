services:
  workplace-booking:
    build: .
    container_name: workplace-booking
    restart: unless-stopped
    network_mode: ${DOCKER_NETWORK_MODE:-bridge}
    env_file:
      - .env
    volumes:
      - ./.state:/app/.state
    command: ["python", "-m", "booking_bot"]
